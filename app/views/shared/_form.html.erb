<%= form_for ([@politician, @politician.comments.build]), role: "form" do |f| %>
  <div class="form-group">
    <%= f.label :comment %><br>
    <%= f.text_area :content, class: "form-control"%>
  </div>
  <p>
    <%= f.submit "Submit", class: "btn btn-default" %>
  </p>
 <% end %>
