<%= @politician.name %>

<%= render 'shared/form'%>

<ul>
<% @politician.comments.each do |comm| %>
  <% next unless comm.persisted?%>
  <img src="/images/up_vote.jpeg" alt="up_vote"><li><%= comm.user.username %>:<br><%= comm.content %></li>
<% end %>
</ul>

